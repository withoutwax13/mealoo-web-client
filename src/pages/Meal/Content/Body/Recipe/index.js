import React from 'react'
import PropTypes from 'prop-types'

const Recipe = (props) => {

	{/* recipeData: { ingredients: [], cookingProcess: [] } */}

	return (
		<div>Recipe</div>
	)
}

Recipe.propTypes = {
	recipeData: PropTypes.object
}

export default Recipe